Given a Matrix where all the rows are sorted.
we have to find the median if all the elements are put together in a data structure in a sorted way
we have to find median in O(log(N*M)) time complexity.